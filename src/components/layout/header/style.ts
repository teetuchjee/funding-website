import styled from 'styled-components'

export const StyleHeaderMenu = styled.div`
  .header-grid {
    display: flex;
  }
  .ml-auto {
    margin-left: auto;
  }
  .header-menu-lang {
    margin-left: auto;
    cursor: pointer;
  }
  .grid-lang-menu {
    display: flex;
    align-items: center;
  }
  .grid-lang-menu div:nth-last-child(1) {
    margin-left: 4px;
  }

  .profile-menu {
    cursor: pointer;
  }
`
